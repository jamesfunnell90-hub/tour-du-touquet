# Tour du Touquet

Read `BRIEF.md` first: it holds the current state, the confirmed weekend formats, the tasks and the acceptance tests.
Visual reference: `docs/mockup.html`.

- No build step: plain ES modules served as-is by GitHub Pages.
- Scoring maths lives only in `js/engine.js` (pure functions). Test with `node --test tests/`.
- Keep demo mode (no Firebase config) working.
- UK English in the UI. No em dashes in UI copy.
